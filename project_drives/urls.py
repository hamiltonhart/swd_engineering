from django.urls import path
from . import views

app_name = "project_drives"

urlpatterns = [
    # path('remove/<int:pk>/', views.project_drive_delete, name="project_drives_delete"),
    # path('remove/<int:pk>/', views.delete_project_drive, name="project_drives_delete"),
]